apply from: "$rootDir/common-android-library.gradle"

android {
    resourcePrefix 'form_'
}

dependencies {
    rootProject.androidLibs.each {
        add(it.config, it.dependency)
    }
    rootProject.formLibs.each {
        add(it.config, it.dependency)
    }
}
